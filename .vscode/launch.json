{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "tsc build",
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "args": ["-dev"]
        }
    ]
}